<!--pages/audioplayer/audioplayer.wxml-->

  <live-player
    id="{{user}}"
        autoplay
        mode="RTC"
        min-cache="0.1"
        max-cache="0.3"
        src="{{playUrl}}"
        background-mute="{{true}}"
        bindstatechange="onPlay"
        >
        
  </live-player>